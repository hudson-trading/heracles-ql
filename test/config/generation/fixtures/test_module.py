from heracles import config

# this is here to test that the HeraclesProject can find bundles via import


rules_a = config.RuleBundle(name="test.rules_a")
rules_b = config.RuleBundle(name="test.rules_b")
